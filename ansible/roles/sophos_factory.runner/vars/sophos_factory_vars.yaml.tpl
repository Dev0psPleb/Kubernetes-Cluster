---
# Sophos Factory Variables -- ./vars/sophos_factory_vars.yaml
agent_id: ${agent_id}
agent_key: ${agent_key}
log_path: ${log_path}
log_filename_pattern: ${log_filename_pattern}
log_retention: ${log_retention}
log_level: ${log_level}
workspace_path: ${workspace_path}